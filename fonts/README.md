# Runtime Fonts Directory

This directory is intentionally empty in source control.

Use one of these scripts to download runtime fonts when needed:

- `./scripts/download-fonts.sh --dest ./fonts`
- `./scripts/download-fonts.ps1 -Dest ./fonts`

Do not commit downloaded font binaries.
